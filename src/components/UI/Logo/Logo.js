import React from 'react';

import appLogo from '../../../assets/images/logo.png';

const logo = () => {
    return (
        <div>
            <img src={appLogo} alt="Seek Logo"/>
        </div>
    );
};

export default logo;
